version: '3'

services:
  db:
    image: mariadb:latest
    ports: ["3306:3306"]
    environment:
     - MYSQL_ROOT_PASSWORD=root
     - MYSQL_USER=root
     - MYSQL_PASSWORD=root
     - MYSQL_DATABASE=rmsdb